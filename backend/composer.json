{
    "name": "artify/backend",
    "description": "Artify — Back-end PHP + MongoDB",
    "type": "project",
    "require": {
        "php": ">=8.1",
        "mongodb/mongodb": "^2.3",
        "vlucas/phpdotenv": "5.6"
    },
    "autoload": {
        "psr-4": {
            "Artify\\": "src/"
        }
    },
    "config": {
        "optimize-autoloader": true
    }
}
